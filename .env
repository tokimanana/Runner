# Environment variables example

# Database connection (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tour_operator?schema=public"

# JWT Security
# Generate a strong key using: openssl rand -base64 32
JWT_SECRET="change_this_to_a_secure_secret_key"
JWT_EXPIRATION="1d"

# API Configuration
PORT=3000
NODE_ENV="development"

# Optional: CORS origins (for frontend access)
CORS_ORIGIN="http://localhost:4200"